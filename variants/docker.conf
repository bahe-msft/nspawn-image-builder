# Docker variant — base system with Docker CE pre-installed

IMAGE_NAME="nspawn-docker"
BASE_DISTRO="noble"
BASE_MIRROR="http://archive.ubuntu.com/ubuntu"
EXTRA_PACKAGES="curl wget vim less htop net-tools iputils-ping dnsutils ca-certificates gnupg lsb-release apt-transport-https software-properties-common iptables"
